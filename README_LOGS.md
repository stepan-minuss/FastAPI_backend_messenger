# Как посмотреть логи сервера

## На Replit:

1. **Консоль Replit:**
   - Логи автоматически отображаются в нижней панели Replit
   - Если панель закрыта, нажмите на иконку консоли внизу экрана
   - Все сообщения с `print()`, `logger.info()`, `logger.error()` и т.д. отображаются там

2. **Уровни логирования:**
   - `DEBUG` - самые подробные логи (все детали)
   - `INFO` - обычная информация (запросы, ответы)
   - `WARNING` - предупреждения
   - `ERROR` - ошибки

## На мобильном устройстве (Flutter):

### Android:
1. **Через Android Studio:**
   - Подключите телефон через USB
   - Откройте Android Studio
   - Выберите устройство в Device Manager
   - Перейдите на вкладку "Logcat"
   - Фильтруйте по `flutter` или `Nebula`

2. **Через ADB (командная строка):**
   ```bash
   adb logcat | grep flutter
   ```

3. **В приложении:**
   - Все логи с `print()` видны в консоли разработчика
   - Если запускаете через `flutter run`, логи видны в терминале

### iOS:
1. **Через Xcode:**
   - Подключите iPhone через USB
   - Откройте Xcode
   - Выберите Window → Devices and Simulators
   - Выберите ваше устройство
   - Нажмите "View Device Logs"

## Что смотреть в логах:

### На сервере (Replit):
- `INFO: GET /users HTTP/1.1 200 OK` - успешные запросы
- `ERROR: Exception in ASGI application` - ошибки
- `socketio_handler - INFO - User X connected` - подключения Socket.IO
- `socketio_handler - INFO - Message saved` - сохранение сообщений

### На клиенте (мобильное приложение):
- `📡 Запрос к...` - HTTP запросы
- `📥 Ответ сервера: статус...` - ответы сервера
- `🔍 Поиск пользователей...` - операции поиска
- `✅ Найден контакт...` - результаты сопоставления контактов
- `📜 Загрузка истории чата...` - загрузка сообщений

## Полезные команды для отладки:

### Просмотр последних логов на сервере:
Логи отображаются в реальном времени в консоли Replit.

### Фильтрация логов:
В Replit можно использовать поиск (Ctrl+F или Cmd+F) для фильтрации логов по ключевым словам:
- `ERROR` - только ошибки
- `Message saved` - сохранение сообщений
- `Connection attempt` - подключения Socket.IO
- `User X` - действия конкретного пользователя

## Включение/выключение подробного логирования:

В файле `backend/main.py` можно изменить уровень логирования:

```python
level=logging.INFO  # Меньше логов (только важное)
level=logging.DEBUG  # Больше логов (все детали)
```

